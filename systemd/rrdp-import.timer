[Unit]
Description=Generate RRDP sync config from rpki-client metrics every hour

[Timer]
# Equivalent to cron: 0 * * * *
OnCalendar=*-*-* *:00:00
# Do not try to catch up missed runs
Persistent=false

[Install]
WantedBy=timers.target
